/**
 * WidgetData interface — shared type for widget props.
 * RexSmallWidget removed; only the type export remains for RexWideWidget.
 */
export interface WidgetData {
  insightIcon: string;
  insightTitle: string;
  insightCategory: string;
  weekCount: number;
}
